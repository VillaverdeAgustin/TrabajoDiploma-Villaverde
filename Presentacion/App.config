<?xml version="1.0" encoding="utf-8" ?>
<configuration>
    <startup>
        <supportedRuntime version="v4.0" sku=".NETFramework,Version=v4.7.2" />
    </startup>
    <appSettings>
        <!-- T03.2 - Clave de la encriptacion reversible del correo del cliente.
             Cambiarla deja ilegibles los correos ya guardados. -->
        <add key="ClaveAES" value="ConectAR-575-AV-2026" />
    </appSettings>
    <connectionStrings>
        <!-- Ajustar Data Source si la instancia de SQL Server tiene otro nombre.
             Base creada por los scripts de db/ (01 a 04). -->
        <add name="ConectAR"
             connectionString="Data Source=localhost\SQLEXPRESS;Initial Catalog=ConectAR_DB;Integrated Security=True"
             providerName="System.Data.SqlClient" />
    </connectionStrings>
</configuration>
